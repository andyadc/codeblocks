package com.andyadc.codeblocks.test.image;

import org.junit.jupiter.api.Test;

public class GenPNG {

	/**
	 * Graphics2D graphics2D = source.createGraphics();
	 * graphics2D.getDeviceConfiguration().createCompatibleImage(source.getWidth(), source.getHeight(), Transparency.TRANSLUCENT);
	 * graphics2D.dispose();
	 */

	@Test
	public void testGen() {
//		BufferedImage image = new BufferedImage();

	}
}
